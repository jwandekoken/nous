{
  "name": "api",
  "scripts": {
    "dev": "uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "install": "command -v uv >/dev/null 2>&1 && uv sync || echo 'uv not found, skipping sync'",
    "lint": "uv run ruff check . && uv run ruff format .",
    "test": "uv run pytest"
  }
}
